{
    "name": "mem stream test",
    "description": "mem stream test",
    "require": {},
    "require-dev": {
        "phpunit/phpunit": "^7"
    },
    "config": {
        "bin-dir": "vendor/bin/"
    }
}
